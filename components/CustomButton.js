import React from 'react';

const CustomButton = ({
    text,
    color,
    onClick,
    className
                      }) => {
    return (
        <button > {text}</button>
    );
};

export default CustomButton;
